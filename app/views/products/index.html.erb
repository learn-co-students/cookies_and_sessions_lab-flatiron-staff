<ul>
  <% @cart.each do |product| %>
    <li><%= product %></li>
  <% end %>
</ul>

<h1>Products for cart - add</h1>

<%= form_tag '/' do %>
  <%= label_tag :product, 'product' %>
  <%= text_field_tag :product %>
  <%= submit_tag "add to cart"%>
<% end %>
